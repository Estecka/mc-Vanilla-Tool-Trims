{
	"type": "minecraft:smithing_transform",
	"base": "${tool_item}",
	"template": "${pattern}_armor_trim_smithing_template",
	"addition": "${material_item}",
	"result": {
		"count": 1,
		"id": "${tool_item}",
		"components": {
			"item_model": "trimmed_${tool_item}/${pattern}",
			"trim": { "pattern": "${pattern}", "material": "${material}" }
		}
	}
}
